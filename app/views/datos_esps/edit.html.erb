#encoding: utf-8
<% provide(:title, 'Editar Datos del espectaculo') %>
<h1>Editar Datos del espectáculo</h1>
<h2>Errores datos del espectáculo</h2>
<% @artistas = @formulario.datos_esp.ficha_artisticas.all %>
<% if (@datos_esp.valid?) %>
	<p>No hay errores en el formulario datos del espectáculo</p>
<% else %>
	<p>
		<b>Cantidad de errores:</b>
		<%= @datos_esp.errors.count %>
	</p>
	<%= @datos_esp.errors.each do |attribute, error| %>
	<% end %>
<% end %>
<div class="row">
  <div class="span6 offset3">
		<%= render 'form' %>
	</div>
</div>
</br>
</br>
<%= link_to t('Volver a campos de datos'), edit_formulario_path(@formulario) %>
